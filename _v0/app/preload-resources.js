'use client'
 
//import ReactDOM from 'react-dom'
 
export function PreloadResources() {
  //ReactDOM.preload('...', { as: '...' })
  /* ReactDOM.preconnect('...', { crossOrigin: '...' })
  ReactDOM.prefetchDNS('ht) */
 
  return null
}